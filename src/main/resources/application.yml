server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/usuarios_microservice?useSSL=false&serverTimezone=UTC
    username: root
    password: ${DB_PASSWORD:rootuser}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
jwt:
  secret: ${JWT_SECRET:MiClaveUltraSeguraCon32Caracteres1234}
